<div class="container">
  <h1><%= @plant.name %></h1>
  <%= simple_form_for [@plant, @plant_tag] do |f| %>
    <%= f.association :tag, input_html: { class: 'select2' } %>
    <%= f.submit %>
  <% end %>
</div>
